# Hardened forwarding tables
# /etc/nftables-hardened.conf

#!/usr/sbin/nft -f

flush ruleset

table inet filter {
    chain input {
        type filter hook input priority filter; policy accept;
    }
 
    chain forward {
        type filter hook forward priority filter; policy drop;

        # Allow established traffic
        ct state established,related accept

        # VLAN 1 to VLAN 2 (Full access)
        iifname "enp0s3.1" oifname "enp0s3.2" accept

        # VLAN 2 to VLAN 1 (Port 80 - Web only)
        iifname "enp0s3.2" oifname "enp0s3.1" tcp dport 80 accept

        # Log and Drop cross-VLAN violations
        iifname "enp0s3.2" oifname "enp0s3.1" log prefix "BLOCKED: " drop
    }
 
    chain output {
        type filter hook output priority filter; policy accept;
    }
}

# Allows VLANs to share the Internet connection
table ip nat {
    chain postrouting {
        type nat hook postrouting priority 100; policy accept;
        oifname "enp0s3" masquerade
    }
}
